{
	"name": "back",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "nodemon -x 'node --openssl-legacy-provider --experimental-specifier-resolution=node index || touch index.js'",
		"dev": "node --experimental-specifier-resolution=node index"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"cors": "^2.8.5",
		"cors-anywhere": "^0.4.4",
		"cross-fetch": "^3.1.5",
		"dotenv": "^16.0.1",
		"express": "^4.18.1",
		"express-handlebars": "^6.0.6",
		"http": "^0.0.1-security",
		"https": "^1.0.0",
		"nodemon": "^2.0.19",
		"pm2": "^5.2.0",
		"request": "^2.88.2"
	},
	"type": "module",
	"engines": {
		"node": "v18.0.0"
	}
}
